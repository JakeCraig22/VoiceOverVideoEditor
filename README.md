# Voice Room

Local reference-video and voiceover editor using React, TypeScript and Vite. No backend ye, uploads, accounts, or cloud resources yet

## Run in VS Code

1. Open file in VSCode (or some IDE)
2. terminal -> new terminal
3. Run `npm install` on first setup, then `npm run dev`.
4. Open the link. 

